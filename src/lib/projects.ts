/**
 * Maps project page slugs to their database IDs.
 * Make sure to seed the `projects` table with matching IDs.
 */
export const PROJECT_IDS: Record<string, number> = {
  "aura-bank": 1,
  "heartguard-ai": 2,
  "pdf-tools": 3,
  "hospital-management": 4,
  "ml-showcase": 5,
};
